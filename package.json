{
  "name": "pharos-contract-inspector",
  "version": "1.0.0",
  "description": "ABI-free EVM contract introspection for the Pharos Agent Center. Point it at any address on Pharos and get a function inventory, proxy resolution, interface detection, and a privileged/danger function report — straight from bytecode, no verified source required.",
  "type": "module",
  "bin": {
    "pharos-inspect": "inspect.js"
  },
  "main": "inspect.js",
  "scripts": {
    "inspect": "node inspect.js",
    "demo": "node inspect.js 0xcfC8330f4BCAB529c625D12781b1C19466A9Fc8B --network testnet",
    "serve": "node server.js",
    "test": "node test-risk.js",
    "lint": "node --check inspect.js && node --check server.js && node --check lib/risk.js && node --check lib/inspect-core.js"
  },
  "keywords": [
    "pharos",
    "agent-center",
    "skill",
    "evm",
    "introspection",
    "abi",
    "proxy",
    "security",
    "bytecode"
  ],
  "license": "MIT-0",
  "dependencies": {},
  "engines": {
    "node": ">=18"
  }
}
